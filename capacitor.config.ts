import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'projeto-recepcao-calouros',
  webDir: 'www'
};

export default config;
